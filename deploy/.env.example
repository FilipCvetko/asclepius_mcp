# Copy to deploy/.env on the server and fill in real values.
# These mirror the five secrets that were set on Fly (`flyctl secrets list`).
# deploy/.env is gitignored — never commit real values.
NOTION_API_KEY=
NOTION_DATABASE_ID=
NOTION_TEMPLATES_DATABASE_ID=
SMTP_USER=
SMTP_PASS=
